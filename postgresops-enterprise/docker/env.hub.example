# Copy to docker/.env.hub (do not commit real production secrets here).
# DOCKERHUB_IMAGE must include tag, e.g. infrayantra/postgresops-control-plane:latest

DOCKERHUB_IMAGE=infrayantra/postgresops-control-plane:latest

# Optional label surfaced in GET /api/v1/meta
POSTGRESOPS_BUILD_ID=docker-hub
