# Python SDK (hand-written)

Generate OpenAPI client later (`openapi-generator-cli`) from `http://localhost:8000/openapi.json`.

For now, call the REST API with `httpx` or `requests` using the documented paths under `/api/v1`.
