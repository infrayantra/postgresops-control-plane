# Ansible

RHEL 8/9 playbooks for:

- Hardened host baseline (FIPS optional)
- Container runtime (podman / containerd)
- Air-gapped artifact sync (RPM + wheel mirror)

Phase 1 contains documentation only; playbooks arrive with packaging hardening.
