# PostgresOps Enterprise — Web Console

React + TypeScript + Vite + Tailwind + TanStack Query.

## Develop

```bash
npm install
npm run dev
```

Ensure the FastAPI backend is listening on `http://127.0.0.1:8000` (Vite proxies `/api`).

## Build

```bash
npm run build
```
